package basics;

public class Strings {
	public static void main(String[] args) {
		String bookTitle, wordChoice;
		wordChoice = "ring";
		bookTitle = "The Lord of the Rings";
		
		if (bookTitle.equalsIgnoreCase(wordChoice)){
			System.out.println("FOUND!");
		}
		
		//wC = wordChoice.toLowerCase()
}
}
